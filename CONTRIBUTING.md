# Contributing

Run checks before committing:

```bash
make validate
make test
```

Do not commit `docs/superpowers/`; those files are local planning artifacts.

## Content rules

All built-in content must be original. Do not copy or closely paraphrase LeetCode, HackerRank, CodeSignal, editorials, examples, tests, or distinctive problem stories.
