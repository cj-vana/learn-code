# Runner Security

The runner is practical local containment, not a hosted hostile multi-tenant sandbox. Runner-broker is trusted local infrastructure and is the only service with Docker socket access.
