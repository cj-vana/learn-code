## What this changes

## Checklist

- [ ] This PR targets the `dev` branch (not `main`)
- [ ] `make validate` and `make test` pass locally
- [ ] Content changes (if any) are original and pass `validate-content`
- [ ] Paths regenerated (`scripts/generate_paths.py`) if content was added
